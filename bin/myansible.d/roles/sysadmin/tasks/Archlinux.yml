- pacman:
    name:
      - clusterssh
      - dnsutils
      - openssh
      - tree
      - wget

      # For upgrading mirror lists
      - reflector

      - remmina
      # Protocol-specific libraries
      - freerdp
      - libvncserver

  become: true
